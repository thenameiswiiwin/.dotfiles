#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

echo "Installing rofi (Linux only)..."
if [[ "$(uname)" == "Linux" ]]; then
    sudo apt update && sudo apt install -y rofi
else
    echo "Not on Linux. Skipping rofi installation."
fi
